{
  "name": "template",
  "version": "1.0.0",
  "description": "Email template",
  "private": true,
  "scripts": {
    "mjml": "mjml --read mjml/*.mjml -o html",
    "watch": "mjml --watch mjml/*.mjml -o html",
    "help": "mjml --help"
  },
  "dependencies": {
    "mjml": "^4.11.0"
  }
}
